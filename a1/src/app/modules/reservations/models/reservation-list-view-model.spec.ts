import { ReservationListViewModel } from './reservation-list-view-model';

describe('ReservationListViewModel', () => {
  it('should create an instance', () => {
    expect(new ReservationListViewModel()).toBeTruthy();
  });
});
